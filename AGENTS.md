# 项目开发协作规范 (Development Rules)

本规则适用于本项目的全部代码协作与文件处理流程，AI 助手在每次交互中均需严格遵守以下原则：

## 1. 代码修改前置声明与用户确认
- **操作前声明**：每次修改或编辑代码前，必须先明确向用户说明将要修改的文件路径、具体位置（类/方法）以及修改原因与方案。
- **确认后执行**：必须等待用户明确回复同意/确认后，才能在下一步真正执行修改，严禁未确认擅自改动。
- **严格依案执行**：严格按照讨论和确定的方案推进，严禁私自绕道走捷径；若有更快捷/复用的替代方案，必须在一开始就清晰、完整地告知用户并征询同意。
- **精准增量修改**：优先使用局部定位替换，避免全量重写。

## 2. 架构命名与代码注释保护
- **注释保留**：严格保留代码中现有的所有代码注释、文档注释（Javadoc）及中文说明。
- **风格统一**：沿用并维护现有的类名、包名、变量命名及架构设计风格。
- **禁止随意重构**：严禁随意重构或更改用户未明确要求的现有方法与逻辑。

## 3. 高危操作确认（删除与覆盖）
- **显式询问**：所有涉及**删除文件**、**覆盖现有文件**（Overwrite）或可能导致数据丢失的破坏性操作，必须在执行前明确向用户说明并征询同意。
- **Git 与终端安全**：不得擅自执行强制推送（force push）、重置（reset）或未授权的跨目录操作。
